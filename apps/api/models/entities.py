"""SHARED backend models. Both BE lanes import these — coordinate before editing.

Skeleton only. BE #1 owns the real schema + migrations (see PRD §6).
"""
# TODO(BE#1): SQLAlchemy models — canvases, source_entities, canvas_objects,
# object_edges, chat_threads, ai_proposals. Add pgvector column for embeddings.
